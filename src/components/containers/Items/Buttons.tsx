import React from 'react'
import { Text, View, Button } from "react-native";
const Buttons = () => {
    return (
        <View>
            <Button
                onPress={() => {
                    
                    }
                }
                title="Cart"
            />
            <Button
                onPress={() => {
                    }
                }
                title="Wish List"
            />
        </View>
    );
}

export default Buttons
